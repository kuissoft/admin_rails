<%= form_for(@token) do |f| %>
  <% if @token.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@token.errors.count, "error") %> prohibited this token from being saved:</h2>

      <ul>
      <% @token.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :token_id %><br>
    <%= f.text_field :token_id %>
  </div>
  <div class="field">
    <%= f.label :user %><br>
    <%= f.collection_select :user_id, User.all, :id, :name  %>
  </div>
  <div class="field">
    <%= f.label :session %><br>
    <%= f.collection_select :session_id, Session.all, :id, :session_id, {:prompt => 'New'} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
